{
  "name": "youtube-transcript-ai-summarizer",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "A local Chrome extension that summarizes YouTube transcripts using OpenAI.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.js",
    "test:browser": "node tests/browser-smoke.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
